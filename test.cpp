#include <iostream>
#include <iomanip>
using namespace std;

int main(){


    // double x, y;
    // x = (double)5+6/11;
    // y = (double)(5+6/11);
    // cout << x << ' ' << y << endl;
    // cout <<( 5 > 3 && 2 || 8 < 4);


    // m >= 10 && m <= 99;
    // a >= 'a' && a <= 'z' || a >= 'A' && a <= 'Z';
    // m % 2 == 1;
    // x != 2 && x != 3;

    
    return 0;

    
}